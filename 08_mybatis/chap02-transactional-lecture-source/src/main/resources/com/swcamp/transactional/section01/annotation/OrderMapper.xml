<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swcamp.transactional.section01.annotation.OrderMapper">
    <insert id="insertOrder" parameterType="com.swcamp.transactional.section01.annotation.Order">
<!--        resultType을 적을 필요 x 적으면 에러남, 매개변수를 받아야함-->
        INSERT
            INTO TBL_ORDER
            (
              ORDER_DATE
            , ORDER_TIME
            , TOTAL_ORDER_PRICE
            )
            VALUES
            (
              #{orderDate}
            , #{orderTime}
            , #{totalOrderPrice}
            )
<!--        pk=resultType 이번 프로젝트는 어지간해서 pk id 값 auto돌리기-->
        <selectKey keyProperty="orderCode" order="AFTER" resultType="_int">
<!--            SELECT MAX(ORDER_CODE) FROM TBL_ORDER-->
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>
<!--result type 필요없음 <= insert-->
    <insert id="insertOrderMenu"
            parameterType="com.swcamp.transactional.section01.annotation.OrderMenu">
        INSERT
        INTO TBL_ORDER_MENU
        (
        ORDER_CODE
        , MENU_CODE
        , ORDER_AMOUNT
        )
        VALUES
        (
        #{orderCode}
        , #{menuCode}
        , #{orderAmount}
        )
    </insert>

</mapper>