server:
  port: 8000

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      routes:
        #        - id: 1st-service-get
        ##          uri: http://localhost:52070/
        #
        #          # ??? ? ???? ???(?, application name? ??(??? ??? ????? ?? ?? ??? ??))
        #          uri: lb://1ST-SERVICE
        #
        #          # ????? ??? ??? ?? ???? ???(??? ???)
        #          predicates:
        #            - Path=/first-service/**
        #
        #          # ?? ??? ? ???? ???? //first-service?? ???? ???? ?? ????? ???? ? + ?? ??
        #          # ???? built in ?? + custom ??(feat.spring security) ?? ??
        #          filters:
        #            - RewritePath=/first-service/(?<segment>.*), /$\{segment}
        #            - AddRequestHeader=first-request, first-request-header
        #            - AddResponseHeader=first-response, first-response-header
        #
        #        - id: 2st-service-get
        #          uri: lb://2ND-SERVICE
        #          predicates:
        #            - Path=/second-service/**
        #          filters:
        #            - RewritePath=/second-service/(?<segment>.*), /$\{segment}

        # ??. gateway? routes ??? ?? ??(? ??? http method ?? ??)? ?? ?? ????.
        - id: user-service-post-user
          uri: lb://SWCAMP-USER-SERVICE
          predicates:
            - Path=/user-service/**
            - Method=POST
          filters:
            - RewritePath=/user-service/(?<segment>.*), /$\{segment}

        - id: user-service-get-user
          uri: lb://SWCAMP-USER-SERVICE
          predicates:
            - Path=/user-service/**
            - Method=GET
          filters:
            - RewritePath=/user-service/(?<segment>.*), /$\{segment}
            - AuthorizationHeaderFilter

        - id: order-service-get
          uri: lb://SWCAMP-ORDER-SERVICE
          predicates:
            - Path=/order-service/**
            - Method=GET
          filters:
            - RewritePath=/order-service/(?<segment>.*), /$\{segment}
            - AuthorizationHeaderFilter

token:
  expiration_time: 43200000
  secret: Hl8zctv7Zt9fO5D8lw8wzHxKFAL+VIls6mtPyoLjpnDaGAauQC8pLHree6IMSGWHZoLj6Fwmna+d02V7Wk6qIQ==